// Use PascalCasing for typescript
// PascalCasing is Capitalize the first letter of every word
function Message()
{
    // The below code is an example of JSX: JavaScript XML
    const name = "screeee";
    if (name)
        return <h1>Dynamically saying {name}</h1>;
    return <h1>No name loser stinky</h1>
}

export default Message;